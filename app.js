const pages = document.querySelectorAll('.page'), navLinks = document.querySelectorAll('.nav-links li'), mobileMenuBtn = document.querySelector('.mobile-menu-btn'), navLinksContainer = document.querySelector('.nav-links'), modal = document.getElementById('write-modal'), toast = document.getElementById('toast'), ICONS = { user: '<svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>' }; let currentPosts = []; document.addEventListener('DOMContentLoaded', () => { initNavigation(); loadContent() }); function initNavigation() { navLinks.forEach(e => { e.addEventListener('click', () => { let t = e.getAttribute('data-target'); navigateTo(t), window.innerWidth <= 768 && navLinksContainer.classList.remove('active') }) }), mobileMenuBtn.addEventListener('click', () => { navLinksContainer.classList.toggle('active') }), document.querySelector('.close-modal').addEventListener('click', () => { modal.classList.remove('show') }), window.addEventListener('click', e => { e.target === modal && modal.classList.remove('show') }), document.getElementById('signup-form').addEventListener('submit', e => { e.preventDefault(), showToast('가입 신청 완료!'), e.target.reset() }), document.getElementById('write-form').addEventListener('submit', async e => { e.preventDefault(); let t = e.target.querySelector('input').value, n = e.target.querySelector('textarea').value; await addPost(t, n), modal.classList.remove('show'), showToast('게시글 등록 완료'), e.target.reset() }) } function navigateTo(e) { navLinks.forEach(t => { t.getAttribute('data-target') === e ? t.classList.add('active') : t.classList.remove('active') }), pages.forEach(t => { t.id === e ? t.classList.add('active') : t.classList.remove('active') }), window.scrollTo(0, 0) } async function loadContent() { try { const e = await fetch(`${CONFIG.API_URL}?id=${DB_ID}`), t = await e.json(); currentPosts = t.results.map(e => ({ id: e.id, title: e.properties.Title.title[0]?.plain_text || "제목 없음", content: e.properties.Content.rich_text[0]?.plain_text || "", author: e.properties.Author.rich_text[0]?.plain_text || "익명", date: e.properties.Date.date?.start || new Date().toISOString().split('T')[0] })), renderPosts() } catch (e) { console.error(e), loadMockCommunity() } loadMockGallery() } function loadMockGallery() { let e = document.getElementById('gallery-container'); e.innerHTML = '';[{ url: 'https://picsum.photos/300/300?random=1', title: '체육대회' }, { url: 'https://picsum.photos/300/300?random=2', title: '현장학습' }, { url: 'https://picsum.photos/300/300?random=3', title: '급식실' }, { url: 'https://picsum.photos/300/300?random=4', title: '쉬는 시간' }, { url: 'https://picsum.photos/300/300?random=5', title: '과학실' }, { url: 'https://picsum.photos/300/300?random=6', title: '방과후' }].forEach(t => { let n = document.createElement('div'); n.className = 'gallery-item', n.innerHTML = `<img src="${t.url}" alt="${t.title}" loading="lazy"><div class="gallery-overlay"><h4>${t.title}</h4></div>`, e.appendChild(n) }) } function loadMockCommunity() { let e = document.getElementById('community-container'); 0 === currentPosts.length && (currentPosts = [{ id: 1, title: '연동 대기중', author: '시스템', date: '2026-03-15', content: 'config.js에 아이디가 올바르게 설정되었는지 확인해주세요.' }]), renderPosts() } function renderPosts() { let e = document.getElementById('community-container'); if (e.innerHTML = '', 0 === currentPosts.length) { e.innerHTML = '<div class="empty-state"><p>게시글이 없습니다.</p></div>'; return } currentPosts.forEach(t => { let n = document.createElement('div'); n.className = 'community-item', n.innerHTML = `<h4>${t.title}</h4><div class="meta"><span>${ICONS.user} ${t.author}</span> &bull; <span>${t.date}</span></div><p style="margin-top: 10px; color: var(--text-light); font-size: 0.95rem;">${t.content}</p>`, e.appendChild(n) }) } async function addPost(e, t) { try { await fetch(`${CONFIG.API_URL}?id=${DB_ID}`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ Title: e, Content: t, Author: "익명", Date: new Date().toISOString().split('T')[0] }) }); await loadContent() } catch (e) { console.error(e) } } function openWriteModal() { modal.classList.add('show') } function showToast(e) { let t = document.getElementById('toast-message'); t.textContent = e, toast.classList.add('show'), setTimeout(() => { toast.classList.remove('show') }, 3e3) }
